通常是一维数组，要寻找任一个元素的右边或者左边第一个比自己大或者小的元素的位置，此时我们就要想到可以用单调栈了

```java
stack<int> st;
for (遍历集合中元素)
{
	if (栈空 || 栈顶元素 >= 当前元素)
	{
	   入栈;
	} 
	else
	{
		while (栈不为空 && 栈顶元素 < 当前元素)
		{
			栈顶元素出栈;
			根据业务场景计算结果;
		}
		当前元素入栈;
	}
}
```

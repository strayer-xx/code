```java
void dfs()//参数用来表示状态
{
    if(到达终止状态)//递归出口
    {
        ...//根据题意来添加
        return；
    }

    if(越界或者不合法状态) return;
    
    for(扩展方式)
    {
        if(扩展方式所达到的合法状态)
        {
            ...//根据题意来添加
            vis[i]=1;//标记
            修改（剪枝）
            dfs();
            vis[i]=0;
            //是否还原标记根据题意来
            //如果加上还原标记，就是回溯算法
        }
    }
    
}
```